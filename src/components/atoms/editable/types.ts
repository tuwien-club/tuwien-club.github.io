interface EditableProps {
  bifrostOptions: {
    pageId: string
    pageName: string
    fieldName: string
    blockId?: number
    blockPosition?: number
    blockType?: string
  }
}

export type {EditableProps}
