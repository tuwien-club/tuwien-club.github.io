// import {pageFields_pagesQuery} from 'drop/lib/types.gen'

// export type BifrostPage = pageFields_pagesQuery

// export type PageNode = Omit<BifrostPage, 'children'> & {
//   children?: PageNode[]
// }
